import { useEffect, useState } from "react"

export default function Dashboard(){


    useEffect(() => {
        //  just check the authorization header in the localstorage if exits then  you are good to go.
        
    }, [])

    return (
        <div>

        </div>
    )
}